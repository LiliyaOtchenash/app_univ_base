<html>
  <head>
    {% if title %}
    <title>{{title}}</title>
    {% else %}
    <title>university</title>
    {% endif %}
  </head>
  <body>
    <h4>{{user.hat_of_table}}:</h4>
    {% for cor in cors %}
        <p>{{cor}}</p>
        {% if cor %}
            <p>{{post.title}} runs: {{cor.first_name}}</p>
        {% endif %}
    {% endfor %}
  </body>
</html>
