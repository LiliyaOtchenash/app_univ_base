<html>
  <head>
    {% if title %}
    <title>{{title}}</title>
    {% else %}
    <title>university</title>
    {% endif %}
  </head>
  <body>
    <h4>{{user.hat_of_table}}:</h4>
    {% for stu in stud %}
    <p>{{stud}}</p>
    {% endfor %}
  </body>
</html>
